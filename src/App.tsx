import GestureParticles from './components/GestureParticles';

function App() {
  return (
    <main className="w-full h-screen bg-black">
      <GestureParticles />
    </main>
  );
}

export default App;
